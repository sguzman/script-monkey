# Extensions

Small browser extensions that do not justify standalone repositories can live here as isolated subprojects.

Each extension should keep its own manifest, source tree, package metadata, build scripts, and documentation inside its directory. No shared build system is required.

Substantial extensions with their own architecture or release identity should remain standalone repositories.
